# --- Python / Flask ---

# Virtual Environments
venv/
.venv/
env/
ENV/
venv.bak/
.env # General dotenv files (often contain secrets)

# Flask instance folder specific ignores
instance/.env # User specified - Contains sensitive configuration
instance/flask.cfg # Often contains sensitive configuration like SECRET_KEY
# instance/ # Uncomment this if the *entire* instance folder should be ignored (less common)

# Compiled Python files
*.pyc
*.pyo
*.pyd
__pycache__/
.python-version # pyenv version file

# Distribution / packaging
*.egg-info/
.eggs/
*.egg
dist/
build/
develop-eggs/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
*.tar.gz
*.whl

# Databases (e.g., local SQLite)
*.sqlite3
*.db
*.sqlite

# Test & Coverage Reports
.coverage
.coverage.*
htmlcov/
nosetests.xml
coverage.xml
pytestdebug.log
.pytest_cache/

# Type checker cache (mypy, pytype)
.mypy_cache/
.pytype/
.dmypy.json
dmypy.json


# --- Ansible ---

*.retry # Ansible retry files generated on failure
# Add specific names of your vault password files if you use them
# .vault_pass.txt
# vault.pass


# --- AWS / Cloud ---

# AWS credentials and config (Critical - should ideally live outside the project!)
.aws/

# Terraform state files (if using Terraform for AWS infra)
.terraform/
terraform.tfstate
terraform.tfstate.backup
*.tfvars # Often contain sensitive variables, commit .tfvars.example instead
terraform/set_env.sh
terraform.tfvars


# AWS CDK output (if using CDK)
cdk.out/

# Serverless Framework artifacts (if using Serverless Framework)
.serverless/


# --- Docker ---

# Add files generated by Docker/Compose locally if needed
# docker-compose.override.yml # Often used for local dev overrides with secrets
# docker/logs/ # If you map container logs locally
*.log # General log files which might be generated by containers or compose


# --- Jenkins ---

# Jenkins usually operates outside the repo directory.
# However, if Jenkinsfile generates local artifacts or caches:
# target/ # Common for Java builds triggered by Jenkins, might apply
# build/ # Might be generated by build steps


# --- IDEs / Editors ---

# VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# PyCharm / IntelliJ
.idea/
*.iml

# Sublime Text
*.sublime-workspace
*.sublime-project

# Atom
.atom/

# Emacs / Vim backup/swap files
*~
*.swp
*.swo


# --- OS Specific (Ubuntu/Linux/macOS/Windows) ---

# Linux/macOS temporary files
.DS_Store
._*
.Spotlight-V100
.Trashes

# Windows thumbnail cache
Thumbs.db
ehthumbs.db
Desktop.ini


# --- General ---

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data / Sensitive files (add specific patterns as needed)
*.key
*.pem
secrets.*
credentials.*
*.local # Common suffix for local config overrides

# Downloaded Dependencies (if using package managers other than pip)
# node_modules/
# bower_components/
# vendor/

# Temporary files
tmp/
temp/
*.tmp
*.bak
*.backup